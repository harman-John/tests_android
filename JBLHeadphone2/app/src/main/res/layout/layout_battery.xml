<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linear_layout_home_battery"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    android:gravity="center">

    <ImageView
        android:id="@+id/batteryChargeImage"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginRight="4dp"
        android:src="@mipmap/ic_charge" />

    <ProgressBar
        android:id="@+id/progress_bar_battery"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="55dp"
        android:layout_height="4dp"
        android:max="100"
        android:maxHeight="4dp"
        android:minHeight="4dp"
        android:progress="0"
        android:progressDrawable="@drawable/horizontal_progress_not_charge" />

    <jbl.stc.com.view.CustomFontTextView
        android:id="@+id/text_view_battery_level"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:layout_marginStart="7dp"
        android:text="0%"
        android:textColor="@color/eq_panel_name_text"
        android:textSize="12sp" />
</LinearLayout>