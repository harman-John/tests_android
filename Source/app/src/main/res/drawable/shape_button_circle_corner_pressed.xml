<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/app_button_background" />
    <corners android:radius="@dimen/app_button_corner_radius" />
    <stroke
        android:width="1dp"
        android:color="@color/main_color" />
</shape>